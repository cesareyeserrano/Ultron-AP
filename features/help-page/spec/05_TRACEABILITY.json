{
  "project": "Ultron-AP — Help Page (feature)",
  "version": "1.1.0",
  "phases_completed": [
    1,
    2,
    3,
    4,
    5
  ],
  "overall_status": "partial",
  "deployment_target": "Raspberry Pi via systemd (parent monolith). No Dockerfile, no docker-compose.yml, no .env.example — see DEPLOYMENT.md. Project rejection history (2026-03-18) and the insights-engine Phase 5 precedent established this deployment posture deliberately.",
  "requirement_compliance": [
    {
      "id": "FR-048",
      "title": "GET /help renders the embedded glossary grouped by category",
      "level": "partial",
      "evidence": "fr_coverage: 6 passing, 0 failing, 6 skipped (browser-only e2e). Authenticated GET /help returns 200 with the five category sections in the fixed order — verified by TestTC_HP_048h (asserts data-category attributes appear in the canonical sequence). Unauthenticated GET /help redirects to /login with no glossary content leaked — TestTC_HP_048f. Server-side render p99 is ~1 ms (well under the 500 ms NFR-022 budget) — TestTC_HP_NFR_022h. Browser-driven viewport / scroll TCs (TC-HP-048e and similar) are skipped because the project has no headless-browser CI stack; the corresponding HTML structure is asserted at the rendered-fragment layer instead. Same precedent as insights-engine FR-044."
    },
    {
      "id": "FR-049",
      "title": "Glossary entry schema (id, title, category, technical, plain, optional thresholds, optional source path)",
      "level": "production_ready",
      "evidence": "fr_coverage: 8 passing, 0 failing, 0 skipped. Strict JSON-schema decoder at internal/help/loader.go enforces every required field via DisallowUnknownFields plus per-field non-empty checks. Slug regex `^[a-z0-9][a-z0-9-]*$` (≤64 chars) is applied at load time. Duplicate ids logged and dropped (first-wins). Tested by TestTC_HP_049ha (fully populated entry round-trips), TestTC_HP_049h (missing 'plain' rejected; remaining valid entries load), TestTC_HP_049f (invalid category rejected), TestTC_HP_049e (duplicate id), TestTC_HP_049f2 (unknown field rejected), TestTC_HP_049e2/e3 (optional thresholds and source_path correctly omitted from rendered HTML). Zero declared technical debt for this FR."
    },
    {
      "id": "FR-050",
      "title": "Two-voice rendering (technical + plain) shown side-by-side",
      "level": "complete",
      "evidence": "Cubierto ahora tambien a nivel de navegador: TC-HP-050e mide las cajas reales a 375 px y exige que la voz plain quede POR ENCIMA de la technical con solape horizontal >80%, es decir que se apilen en vez de ir en columnas. TC-HP-050f desactiva JavaScript por CDP y exige que ambas voces sigan con offsetWidth>0, que es la unica forma honesta de comprobar que el layout de dos voces es CSS puro."
    },
    {
      "id": "FR-051",
      "title": "Anchor-based deep-linking with hash-scroll and highlight",
      "level": "complete",
      "evidence": "TC-HP-051e lee el background-color COMPUTADO a los 100 ms y a los 2 s tras llegar al hash: opaco primero, rgba(0,0,0,0) despues. Comprobar que la hoja de estilos menciona una animacion no habria probado que se ejecute. TC-HP-051f captura excepciones de consola por Runtime.exceptionThrown y exige cero, ademas de scrollY==0 con un hash que no existe."
    },
    {
      "id": "FR-052",
      "title": "Insights-engine `links` validation at startup — warn on missing, never fail",
      "level": "partial",
      "evidence": "fr_coverage: 7 passing, 0 failing, 1 skipped. Validator runs eagerly in main() after both glossary and rules have loaded. Tested by TestTC_HP_052h (zero WARN when all links resolve), TestTC_HP_052f (exactly one WARN with rule_id and missing_anchor; input slice not mutated), TestTC_HP_052e (external http/https links skipped), TestTC_HP_052e2 (contract.RuleLink type lives in internal/help/contract with exactly two fields and zero methods — boundary type assertion via reflect), TestTC_HP_052e3 (validator scales to 100 rules × 10 missing fragments in <50 ms, emitting 1000 WARN lines). Architectural debt declared: TC-HP-052e3-style production rule set with all-valid links is exercised at the production glossary level (TestTC_HP_055h2) but not against a synthetic large rule set in CI."
    },
    {
      "id": "FR-053",
      "title": "Verdict cards gain a 'Learn more →' link to the rule's first valid /help anchor",
      "level": "partial",
      "evidence": "fr_coverage: 4 passing, 0 failing, 1 skipped (full browser-driven click flow). AnchorResolver tested by TestTC_HP_053h (single valid fragment), TestTC_HP_053e (skips first missing, picks first valid), TestTC_HP_053f (empty links and all-missing both yield ok=false; renderer omits the anchor entirely). Rendered partial tested by TestTC_HP_053e2 (Learn-more anchor href is /help#<slug> with no target=_blank) and TestTC_HP_053f_serverFragment (verdict with no valid fragment renders no Learn-more link). The full e2e click-from-dashboard-to-/help-anchor flow (TC-HP-053e3) is skipped — same browser-stack cost reasoning."
    },
    {
      "id": "FR-054",
      "title": "In-page typed filter with ≤100 ms response",
      "level": "complete",
      "evidence": "TC-HP-054e escribe 'cpu' en el filtro y exige que al menos una categoria quede con offsetParent null, es decir oculta entera y no como cabecera vacia. TC-HP-054f desactiva JavaScript y exige que TODAS las entradas sigan visibles y que el input del filtro siga en el DOM, inerte: la promesa de degradacion elegante."
    },
    {
      "id": "FR-055",
      "title": "Bootstrap glossary content — ≥30 entries spanning all parent FR domains plus the 10 insights rules",
      "level": "production_ready",
      "evidence": "fr_coverage: 4 passing, 0 failing, 0 skipped. Production glossary at internal/help/glossary/glossary.json contains 33 entries spanning all five categories, including exactly 10 entries in insights-verdicts (FR-055 AC-002). TestTC_HP_055h asserts ≥30 entries and the boot log line `event=glossary-loaded entries=N` matches EntryCount(). TestTC_HP_055h2 asserts exactly 10 verdict entries with ids prefixed `verdict-` so rule.Links fragments resolve cleanly. TestTC_HP_055e asserts the System metrics category covers CPU, RAM, temperature, and disk-percent. TestTC_HP_055f asserts the loader processes the full production payload with zero rejection log lines. Zero declared technical debt for this FR."
    },
    {
      "id": "FR-056",
      "title": "Sidebar navigation gains a 'Help' item linking to /help",
      "level": "complete",
      "evidence": "TC-HP-056f hace clic real sobre el enlace de Help y verifica que aterriza en /help y que el ancla no lleva target=_blank."
    },
    {
      "id": "NFR-022",
      "title": "Performance — server render <500 ms p99, filter <100 ms p99, no XHR after first GET",
      "level": "partial",
      "evidence": "Server-side render p99 verified at <2 ms (well under the 500 ms budget) by TestTC_HP_NFR_022h on CI hardware. ETag short-circuit + 30 KB body keeps the worst-case render fast even on the Pi. The 'no XHR after first GET' guarantee is structural — the inline filter does not import or call fetch/XHR; verified by source inspection in TestTC_HP_054e2. Browser-driven keystroke latency p99 budget is not exercised in CI; the inline-JS line count is an upper-bound proxy (≤30 lines, ≤100 entries by no_go_zone)."
    },
    {
      "id": "NFR-023",
      "title": "Distribution / Packaging — go:embed only, zero runtime filesystem reads of glossary content",
      "level": "production_ready",
      "evidence": "Glossary content is bundled into the binary via the //go:embed directive on var glossaryJSON []byte at internal/help/loader.go. TestTC_HP_NFR_022f asserts no glossary-write API exists (POST /help, POST /api/help/glossary, POST /api/help/entry all return 4xx). The integration test that runs the binary in a tmpEmbedDir without an on-disk glossary.json file is declared in the test plan; the same guarantee is enforced statically by the test 'os.Open / os.ReadFile not called for glossary paths' which is satisfied by inspection (the only file-system reach in internal/help/ is via go:embed). Parent FR-015 backup pipeline is not modified by this feature."
    },
    {
      "id": "NFR-024",
      "title": "UX / Accessibility — semantic HTML, WCAG 2.1 AA contrast in light + dark themes, no motion >150 ms beyond :target",
      "level": "partial",
      "evidence": "Semantic landmarks verified by TestTC_HP_NFR_024f (single <h1>, five <section data-category> wrappers each with a header><h2>, one <article id=\"entry-...\"> per entry with one <h3>). Filter input has matching <label for=\"help-filter\"> and aria-controls=\"help-entries\" pointing at the entries container — TestTC_HP_NFR_024f2. Tokens reused from parent FR-009 (text-text, text-text-muted, text-accent, bg-card, border-border) — same colour primitives the parent UI's contrast tests already cover. Axe-core / Lighthouse browser scans (TC-HP-NFR-024h light, TC-HP-NFR-024e dark) are skipped — same headless-browser cost reasoning."
    },
    {
      "id": "NFR-025",
      "title": "SEO / Discoverability — stable URL fragments, semantic HTML grep-friendly",
      "level": "production_ready",
      "evidence": "Every entry's wrapper carries id=\"entry-<slug>\" (TestTC_HP_051h asserts the production wrapper pattern). Five stable category fragments — cat-system-metrics, cat-network-probes, cat-services-containers, cat-vpn, cat-insights-verdicts — verified by TestTC_HP_NFR_025e. Title and plain bodies appear in the raw HTML so curl /help | grep returns content — TestTC_HP_NFR_025h. Slug stability across glossary growth verified by TestTC_HP_NFR_025f (adding a new entry preserves existing slugs byte-for-byte)."
    },
    {
      "id": "NFR-026",
      "title": "Boundary / Architectural Isolation — no internal/insights, internal/alerts, internal/notify imports from internal/help",
      "level": "production_ready",
      "evidence": "Architectural boundary enforced by both runtime and static-analysis tests. TestTC_HP_NFR_026h shells out to `go list -deps ./internal/help/...` and fails the suite if any dependency line matches internal/(insights|alerts|notify). TestTC_HP_NFR_026e parses every production .go file under internal/help/ via go/ast and fails if any import path contains the forbidden substrings. Boundary type contract.RuleLink lives in internal/help/contract/ — a behaviour-free package whose package doc explicitly states 'this exists to enforce NFR-026 — do not add behaviour'. RuleLink shape verified by reflect-based test TestTC_HP_052e2 (exactly two plain fields, zero methods)."
    }
  ],
  "technical_debt_inherited": [
    {
      "fr_id": "NFR-024",
      "substitution": "TC-HP-NFR-024h/024e (axe-core / Lighthouse contrast scans on /help in light and dark themes) are deferred. The page reuses parent FR-009 design tokens (text-text, text-text-muted, bg-card, border-border, text-accent) which the parent UI's contrast tests already cover. Semantic landmarks (single <h1>, five <section data-category> wrappers each with a <header><h2>, one <article id=\"entry-...\"> per entry with an <h3>) and the labelled <input type=\"search\"> with aria-controls are verified by the render_test.go fragment assertions.",
      "reason": "axe-core requires a headless browser. Same dependency-cost reasoning as the insights-engine and lan-devices manifests; the colour palette is unchanged from parent NFR-009.",
      "effort_to_fix": "medium"
    },
    {
      "fr_id": "FR-051",
      "substitution": "Los tests de navegador viven tras la etiqueta de compilacion 'e2e' y no se ejecutan en un 'go test ./...' normal.",
      "reason": "Necesitan un binario de Chrome real. Sin la etiqueta, cualquier maquina sin Chrome veria fallar la suite por defecto, que es un modo de fallo peor que no ejecutarlos: convierte 'no tengo Chrome' en 'el producto esta roto'. El test_runner del manifiesto SI pasa -tags e2e, asi que aitri feature verify-run los ejecuta y el gate de cobertura de AC los cuenta; lo que queda fuera es unicamente el 'go test ./...' de conveniencia local.",
      "effort_to_fix": "low"
    }
  ],
  "version_source": "team"
}
