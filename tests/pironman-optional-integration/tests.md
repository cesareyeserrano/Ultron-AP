# Test Cases: pironman-optional-integration

> Generated by `aitri plan`.
> Spec-driven rule: every test MUST trace to a User Story (US-*) and one or more Functional Rules (FR-*). Include AC-* when applicable.
> Discovery rigor profile: deep.
> QA rule: Include broad negative, abuse, and boundary coverage from the first implementation slice.
> Quality profile: Game/Interactive (game).

## Functional

### TC-1
- Title: Validate us-1 primary behavior.
- Trace: US-1, FR-1, AC-2
- Steps:
  1) Given el feature flag de Pironman esta deshabilitado
  2) When el usuario navega por Ultron core
  3) Then no se muestra modulo hardware ni se ejecutan operaciones Pironman

### TC-2
- Title: Validate us-2 primary behavior.
- Trace: US-2, FR-2, AC-1
- Steps:
  1) Given un admin autenticado en el modulo opcional de hardware
  2) When edita valores y presiona Apply
  3) Then se ejecuta una sola operacion, el estado transiciona a applied o failed, y la UI nunca queda en busy permanente

### TC-3
- Title: Validate us-3 primary behavior.
- Trace: US-3, FR-3, AC-3
- Steps:
  1) Given Pironman API falla o excede timeout
  2) When se solicita read/apply
  3) Then la integracion reporta `degraded` o `unavailable` y Ultron core permanece operativo

### TC-4
- Title: Validate us-4 primary behavior.
- Trace: US-4, FR-4, AC-4
- Steps:
  1) Given una solicitud invalida de parametros hardware
  2) When llega al helper
  3) Then se rechaza y queda registro auditable del rechazo

## Negative / Abuse

### TC-5
- Title: Handle edge behavior - Timeout o caida de API Pironman durante apply, o clicks repetidos de apply en rafaga.
- Trace: US-1, FR-1
- Steps:
  1) Given timeout o caida de api pironman durante apply, o clicks repetidos de apply en rafaga
  2) When the relevant workflow is executed
  3) Then the system returns a deterministic failure-safe response

## Security

### TC-6
- Title: Enforce security control - Mantener privilegios fuera del web process: helper root como unico boundary, validacion estricta de parametros, CSRF/session obligatorios y logs auditables.
- Trace: US-1, FR-1
- Steps:
  1) Given threat scenario: mantener privilegios fuera del web process: helper root como unico boundary, validacion estricta de parametros, csrf/session obligatorios y logs auditables
  2) When an invalid or abusive action is attempted
  3) Then access is blocked and evidence is logged

## Edge Cases

1. Timeout o caida de API Pironman durante apply, o clicks repetidos de apply en rafaga.
