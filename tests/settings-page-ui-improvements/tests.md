# Test Cases: settings-page-ui-improvements

> Generated by `aitri plan`.
> Spec-driven rule: every test MUST trace to a User Story (US-*) and one or more Functional Rules (FR-*). Include AC-* when applicable.
> Discovery rigor profile: standard.
> QA rule: Include baseline negative and security scenarios before implementation handoff.
> Quality profile: Web/SaaS (web).

## Functional

### TC-1
- Title: Validate us-1 primary behavior.
- Trace: US-1, FR-1, AC-1
- AC: Given an authenticated administrator on the Settings page on a mobile viewport, when the page loads, then the layout is compact, readable, and all primary settings sections are reachable without UI overlap or broken hierarchy.

### TC-2
- Title: Validate us-2 primary behavior.
- Trace: US-2, FR-2, AC-3
- AC: Given an authenticated administrator has typed the required confirmation word for shutdown or restart, when the action is submitted, then a visible countdown cancel window is shown before execution and the action can be canceled during that window.

### TC-3
- Title: Validate us-3 primary behavior.
- Trace: US-3, FR-3, AC-3
- AC: Given an authenticated administrator has typed the required confirmation word for shutdown or restart, when the action is submitted, then a visible countdown cancel window is shown before execution and the action can be canceled during that window.

## Negative / Abuse

### TC-4
- Title: Handle edge behavior - On small mobile screens, long setting labels, helper text, and validation errors can overlap or push critical controls off-screen, causing accidental taps or missed confirmations.
- Trace: US-1, FR-1
- AC: Given on small mobile screens, long setting labels, helper text, and validation errors can overlap or push critical controls off-screen, causing accidental taps or missed confirmations, when the workflow executes, then the system returns a deterministic failure-safe response.

## Security

### TC-5
- Title: Enforce security control - All state-changing settings actions must require a valid authenticated session, CSRF protection, and same-origin validation, and all rejected dangerous-action attempts must be audit-logged.
- Trace: US-1, FR-1
- AC: Given threat scenario: all state-changing settings actions must require a valid authenticated session, csrf protection, and same-origin validation, and all rejected dangerous-action attempts must be audit-logged, when an invalid or abusive action is attempted, then access is blocked and evidence is logged.

## Edge Cases

1. On small mobile screens, long setting labels, helper text, and validation errors can overlap or push critical controls off-screen, causing accidental taps or missed confirmations.
