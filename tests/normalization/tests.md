# Test Cases: normalization

> Generated by `aitri plan`.
> Spec-driven rule: every test MUST trace to a User Story (US-*) and one or more Functional Rules (FR-*). Include AC-* when applicable.
> Discovery rigor profile: quick.
> QA rule: Run smoke + core functional checks first, then expand abuse/edge coverage after discovery refinement.
> Quality profile: Web/SaaS (web).

## Functional

### TC-1
- Title: Validate us-1 primary behavior.
- Trace: US-1, FR-1, AC-1
- AC: Given an authenticated operator on Dashboard, when live telemetry is healthy, then critical status is visible in first viewport and corrective navigation is reachable in two clicks or fewer.

### TC-2
- Title: Validate us-2 primary behavior.
- Trace: US-2, FR-2, AC-2
- AC: Given a state-changing endpoint request, when auth session is invalid or CSRF/origin validation fails, then the request is rejected and an auditable security event is logged.

## Negative / Abuse

### TC-3
- Title: Handle edge behavior - Live data can be delayed or unavailable; UI must show stale-data state with last-update timestamp and recovery path without layout breakage.
- Trace: US-1, FR-1
- AC: Given live data can be delayed or unavailable; ui must show stale-data state with last-update timestamp and recovery path without layout breakage, when the workflow executes, then the system returns a deterministic failure-safe response.

## Security

### TC-4
- Title: Enforce security control - Enforce whitelist-first input validation plus rate limits on auth and high-cost endpoints (including SSE reconnect abuse), and audit-log all rejects.
- Trace: US-1, FR-1
- AC: Given threat scenario: enforce whitelist-first input validation plus rate limits on auth and high-cost endpoints (including sse reconnect abuse), and audit-log all rejects, when an invalid or abusive action is attempted, then access is blocked and evidence is logged.

## Edge Cases

1. Live data can be delayed or unavailable; UI must show stale-data state with last-update timestamp and recovery path without layout breakage.
