# Test Cases: dashboard-stream-efficiency

> Generated by `aitri plan`.
> Spec-driven rule: every test MUST trace to a User Story (US-*) and one or more Functional Rules (FR-*). Include AC-* when applicable.
> Discovery rigor profile: quick.
> QA rule: Run smoke + core functional checks first, then expand abuse/edge coverage after discovery refinement.
> Quality profile: Web/SaaS (web).

## Functional

### TC-1
- Title: Validate us-1 primary behavior.
- Trace: US-1, FR-1, AC-1, AC-3
- AC: Given an authenticated operator on dashboard, when SSE stream runs, then metrics remain frequently updated and charts update at a slower cadence.

### TC-2
- Title: Validate us-2 primary behavior.
- Trace: US-2, FR-2, AC-2
- AC: Given temperature samples in collector history, when charts render, then temperature history chart is visible.

### TC-3
- Title: Validate us-3 primary behavior.
- Trace: US-3, FR-3, AC-3
- AC: Given temperature value in normal/warning/high range, when dashboard renders indicator and chart, then colors are green/yellow/red respectively.

### TC-4
- Title: Validate us-4 primary behavior.
- Trace: US-4, FR-4, AC-1
- AC: Given an authenticated operator on dashboard, when SSE stream runs, then metrics remain frequently updated and charts update at a slower cadence.

## Negative / Abuse

### TC-5
- Title: Handle edge behavior - Temperature sensor unavailable (`nil`): dashboard must render placeholder and avoid panics.
- Trace: US-1, FR-1
- AC: Given temperature sensor unavailable (`nil`): dashboard must render placeholder and avoid panics, when the workflow executes, then the system returns a deterministic failure-safe response.

### TC-6
- Title: Handle edge behavior - Sparse history (few points after startup): charts must render with available points only.
- Trace: US-1, FR-1
- AC: Given sparse history (few points after startup): charts must render with available points only, when the workflow executes, then the system returns a deterministic failure-safe response.

## Security

### TC-7
- Title: Enforce security control - Keep existing privilege separation: web process remains unprivileged and does not execute host-privileged commands.
- Trace: US-1, FR-1
- AC: Given threat scenario: keep existing privilege separation: web process remains unprivileged and does not execute host-privileged commands, when an invalid or abusive action is attempted, then access is blocked and evidence is logged.

### TC-8
- Title: Enforce security control - Do not introduce new external endpoints or unauthenticated routes for dashboard stream changes.
- Trace: US-1, FR-1
- AC: Given threat scenario: do not introduce new external endpoints or unauthenticated routes for dashboard stream changes, when an invalid or abusive action is attempted, then access is blocked and evidence is logged.

## Edge Cases

1. Temperature sensor unavailable (`nil`): dashboard must render placeholder and avoid panics.
2. Sparse history (few points after startup): charts must render with available points only.
