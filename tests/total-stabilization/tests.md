# Test Cases: total-stabilization

> Generated by `aitri plan`.
> Spec-driven rule: every test MUST trace to a User Story (US-*) and one or more Functional Rules (FR-*). Include AC-* when applicable.
> Discovery rigor profile: deep.
> QA rule: Include broad negative, abuse, and boundary coverage from the first implementation slice.
> Quality profile: CLI/Automation (cli).

## Functional

### TC-1
- Title: Validate us-1 primary behavior.
- Trace: US-1, FR-1, AC-1
- Steps:
  1) Given the markdown inventory and reference scan
  2) When documentation hygiene runs
  3) Then every markdown file is classified as keep/consolidate/archive/delete with justification, no broken internal links remain, and files above the defined max length are split or reduced

## Negative / Abuse

### TC-2
- Title: Handle edge behavior - A markdown document appears redundant by title or section overlap but is still referenced by automation/spec workflows; cleanup must detect these references before deletion and preserve traceability.
- Trace: US-1, FR-1
- Steps:
  1) Given a markdown document appears redundant by title or section overlap but is still referenced by automation/spec workflows; cleanup must detect these references before deletion and preserve traceability
  2) When the relevant workflow is executed
  3) Then the system returns a deterministic failure-safe response

## Security

### TC-3
- Title: Enforce security control - State-changing endpoints must enforce CSRF token checks plus same-origin protections (Origin/Referer policy), and session cookies must remain secure under direct TLS and trusted proxy TLS termination.
- Trace: US-1, FR-1
- Steps:
  1) Given threat scenario: state-changing endpoints must enforce csrf token checks plus same-origin protections (origin/referer policy), and session cookies must remain secure under direct tls and trusted proxy tls termination
  2) When an invalid or abusive action is attempted
  3) Then access is blocked and evidence is logged

## Edge Cases

1. A markdown document appears redundant by title or section overlap but is still referenced by automation/spec workflows; cleanup must detect these references before deletion and preserve traceability.
